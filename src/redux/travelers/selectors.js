export const selectTravellers = (state) => state.travelers.list;
export const selectTravellersPage = (state) => state.travelers.page;
export const selectTravellersTotalPages = (state) => state.travelers.totalPages;
export const selectTravellersLoading = (state) => state.travelers.loading;
