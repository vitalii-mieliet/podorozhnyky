tags:
  - Users
summary: Get created (published) stories of the authenticated user
operationId: getCreatedStories
description: Retrieves a list of created (published) stories for the authenticated user with pagination and sorting options.
security:
  - bearerAuth: []
parameters:
  - name: page
    in: query
    description: 'Page number for pagination'
    required: false
    schema:
      type: integer
      example: 1
  - name: perPage
    in: query
    description: 'Number of stories per page'
    required: false
    schema:
      type: integer
      example: 10
  - name: sortBy
    in: query
    description: 'Field to sort by'
    required: false
    schema:
      type: string
      enum:
        - rate
        - date
      example: 'date'
  - name: sortOrder
    in: query
    description: 'Order of sorting'
    required: false
    schema:
      type: string
      enum:
        - asc
        - desc
      example: 'desc'
responses:
  '200':
    $ref: '../../../components/responces/users/save-story/200.yaml'
  '401':
    $ref: '../../../components/responces/stories/401.yaml'
  '404':
    $ref: '../../../components/responces/users/save-story/404.yaml'
